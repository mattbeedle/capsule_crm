language: ruby
rvm:
  - 1.9.3
  - 1.9.2
  - jruby-19mode
  - rbx-19mode
before_install:
  - gem update --system
  - gem install bundler
  - gem install rake
script:
  - bundle exec rspec spec/
bundler_args: --binstubs=./bundler_stubs
